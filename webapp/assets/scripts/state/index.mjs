/**
 * 状态层 barrel 模块
 */
export const Store = window.Store;
export const DataValidator = window.DataValidator;
export const DATA_VERSION = window.DATA_VERSION;
export const DEFAULT_DATA = window.DEFAULT_DATA;
export const createEmptyDayData = window.createEmptyDayData;
